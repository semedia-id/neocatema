{% extends '@nucleus/partials/particle.html.twig' %}

{%- set content = fileget('/'~particle.file) -%}
{%- set text = particle.twig ? gantry.theme.compile(content) : content -%}
{%- set text = (particle.filter ? gantry.platform.filter(text) : text)|html|raw %}

{% block particle %}
{%- if particle.class %}<div class="{{particle.class}}">{% endif -%}
{{ text|raw }}
{%- if particle.class %}</div>{% endif -%}
{% endblock %}