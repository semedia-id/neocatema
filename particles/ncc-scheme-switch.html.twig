{% do gantry.document.addInlineScript("ncc_scheme_actived()", 0, 'footer') %}

{% block particle %}
<button  
	class='toggle gn_scheme_switch'
	title="Scheme" id="ncc_scheme_switch" onclick="ncc_scheme_switch();">
	<i class="ncico-toggle-left"></i>
	{% if particle.title %}<span>{{ particle.title|raw }}</span>{% endif %}
</button>
{% endblock %}