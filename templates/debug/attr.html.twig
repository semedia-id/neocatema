<ul>
{% for attr,value in to_array(segment.attributes) -%}
    {% if value is iterable %}
        {% set res = print_r(value,true) %}
    {% else %}
        {% set res = value %}
    {% endif %}

    <li>{{attr}} = {{res}}</li>
{% endfor %}
</ul> 